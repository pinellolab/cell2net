import pandas as pd

from ._utils import check_if_igraph


def tf_gene_network(net: pd.DataFrame):
    # Import matplotlib
    # plt = check_if_matplotlib(return_mpl=False)
    # mpl = check_if_matplotlib(return_mpl=True)
    ig = check_if_igraph()

    pass
